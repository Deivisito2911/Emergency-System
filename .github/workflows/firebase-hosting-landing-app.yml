# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting Landing App
on:
  push:
    branches:
      - main
    paths:
      - landing-app/**
  pull_request:
    branches:
      - main
    paths:
      - landing-app/**
jobs:
    build:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v3
            with:
              node-version: '18'
          - run: npm install
          - run: npx nx run landing-app:build
    test:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v3
            with:
              node-version: '18'
          - run: npm install
          - run: npx nx run landing-app:test
    lint:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v3
            with:
              node-version: '18'
          - run: npm install
          - run: npx nx run landing-app:lint
  
    build_and_deploy:
        runs-on: ubuntu-latest
        steps:
          
          - uses: actions/checkout@v3
          - run: npm install && npx nx run landing-app:build
          - name: Deploy to Firebase
            if: ${{ github.event_name == 'push' }}
            uses: FirebaseExtended/action-hosting-deploy@v0
            with:
              repoToken: '${{ secrets.GITHUB_TOKEN }}'
              firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMERGENCIASUDOAPP }}'
              channelId: live
              projectId: emergenciasudoapp
              name: landingPage
              target: landingapp
          - name: Deploy to Firebase
            if: ${{ github.event_name == 'pull_request' }}
            uses: FirebaseExtended/action-hosting-deploy@v0
            with:
              repoToken: '${{ secrets.GITHUB_TOKEN }}'
              firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMERGENCIASUDOAPP }}'
              channelId: staging
              projectId: emergenciasudoapp
              name: landingPage
              target: landingapp