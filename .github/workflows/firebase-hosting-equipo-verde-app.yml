# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy Equipo-Verde
on:
  push:
    branches:
      - main
    paths:
      - emergency-control-verde/**
      - back-emeregency-control-verde/**
  pull_request:
    branches:
      - main
    paths:
      - emergency-control-verde/**
      - back-emergency-control-verde/**
jobs:
  build_front:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '20'
      - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
      - run: npm install --include=optional
      - run: npx nx run emergency-control-verde:build

  test_front:
    needs: [build_front]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
      - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
      - run: npm install --include=optional
      - run: npx nx run emergency-control-verde:test

  lint_front:
    needs: [test_front]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
      - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
      - run: npm install --include=optional
      - run: npx nx run emergency-control-verde:lint

  build_back:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '20'
      - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
      - run: npm install --include=optional
      - run: npx nx run back-emergency-control-verde:build

  test_back:
    needs: [build_back]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
      - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
      - run: npm install --include=optional
      - run: npx nx run back-emergency-control-verde:test

  lint_back:
    needs: [test_back]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '18'
      - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
      - run: npm install --include=optional
      - run: npx nx run back-emergency-control-verde:lint

  build_and_deploy:
    needs: [lint_front, lint_back]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
      - run: npm install --include=optional && npx nx run emergency-control-verde:build
      - name: Deploy to Firebase
        if: ${{ github.event_name == 'push' }}
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMERGENCIASUDOAPP }}'
          channelId: live
          projectId: emergenciasudoapp
          name: emergenciasPage
          target: equipo-verde
      - name: Deploy to Firebase
        if: ${{ github.event_name == 'pull_request' }}
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMERGENCIASUDOAPP }}'
          channelId: staging
          projectId: emergenciasudoapp
          name: emergenciasPage
          target: equipo-verde
