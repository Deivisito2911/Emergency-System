# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools


name: Deploy Equipo-Rojo
on:
  push:
    branches:
      - main
    paths:
      - navigation-emergency-rojo/**
  pull_request:
    branches:
      - main
    paths:
      - navigation-emergency-rojo/**
jobs:
    build:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v3
            with:
              node-version: '18'
          - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
          - run: npm install
          - run: npx nx run navigation-emergency-rojo:build
    test:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v3
            with:
              node-version: '18'
          - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
          - run: npm install
          - run: npx nx run navigation-emergency-rojo:test
    lint:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v3
            with:
              node-version: '18'
          - run: npm install
          - run: npx nx run navigation-emergency-rojo:lint
  
    build_and_deploy:
        runs-on: ubuntu-latest
        steps:
          
          - uses: actions/checkout@v3
          - run: npm install && npx nx run navigation-emergency-rojo:build
          - run: npm install @nx/nx-linux-x64-gnu @rollup/rollup-linux-x64-gnu @nx/webpack @nx/cypress
          - name: Deploy to Firebase
            if: ${{ github.event_name == 'push' }}
            uses: FirebaseExtended/action-hosting-deploy@v0
            with:
              repoToken: '${{ secrets.GITHUB_TOKEN }}'
              firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMERGENCIASUDOAPP }}'
              channelId: live
              projectId: emergenciasudoapp
              name: emergenciasPage
              target: equipo-rojo
          - name: Deploy to Firebase
            if: ${{ github.event_name == 'pull_request' }}
            uses: FirebaseExtended/action-hosting-deploy@v0
            with:
              repoToken: '${{ secrets.GITHUB_TOKEN }}'
              firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMERGENCIASUDOAPP }}'
              channelId: staging
              projectId: emergenciasudoapp
              name: emergenciasPage
              target: equipo-rojo